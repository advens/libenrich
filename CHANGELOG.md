# Changelog

## [0.1.0]

- Header-only `.thrt`, overlay, and prevalence formats.
- `thrtutil`, `thrt_cli`, `prev_lookup`, and optional `overlay_tool`.
- `enrich` builds a `.thrt` from a JSON config and can fetch GeoIP and
  plain files. It does not distribute files to edges.
